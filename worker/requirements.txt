# OnlyMyPDF Python worker dependencies.
redis==5.0.7

# PDF / OCR / conversion (wired in Phase 4/5)
pymupdf==1.24.7        # MuPDF — fast reading, detection, rendering
pdfplumber==0.11.1     # text + simple tables
camelot-py==0.11.0     # table extraction (lattice/stream)
ocrmypdf==16.4.1       # searchable PDF (Tesseract under the hood)
pdf2docx==0.5.8        # PDF -> Word fast mode
Pillow==10.4.0
# System packages also required (installed in Dockerfile):
#   libreoffice, poppler-utils, ghostscript, qpdf, tesseract-ocr
